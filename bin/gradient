#!/bin/bash

# Get the directory of the current script (absolute path)
SCRIPT_DIR=$(dirname "$(realpath "$0")")

# Path to the Python script
PYTHON_SCRIPT="$SCRIPT_DIR/../scripts/gradient.py"

# Run the Python script
python3 "$PYTHON_SCRIPT"
